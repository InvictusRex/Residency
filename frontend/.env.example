# Frontend environment variables (see README for full setup)
# Base URL of the FastAPI backend. Development default points at the local backend.
# Production: set to your Cloudflare Tunnel API hostname, e.g. https://api.residency.<domain>
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000